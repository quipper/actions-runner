name: build

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  build-ubuntu22:
    uses: ./.github/workflows/reusable--build.yaml
    permissions:
      contents: read
      packages: write

  build-ubuntu20:
    uses: ./.github/workflows/reusable--build.yaml
    permissions:
      contents: read
      packages: write
    with:
      file: Dockerfile.ubuntu20
      flavor: suffix=-ubuntu20

  e2e-test:
    needs:
      - build-ubuntu22
      - build-ubuntu20
    uses: ./.github/workflows/reusable--e2e-test.yaml
    secrets: inherit
    permissions:
      contents: read
      actions: write
    with:
      runner-image-uri-ubuntu22: ${{ needs.build-ubuntu22.outputs.image-uri }}
      runner-image-uri-ubuntu20: ${{ needs.build-ubuntu20.outputs.image-uri }}
